package br.com.vinicius.Semana2.Vetores;

import br.com.vinicius.Semana2.*;

public class Main {


        /*double[] temperaturas = new double[10];

        double temperaturaDia2 = 25.6;
        double temperaturaDia3 = 17.5;
        double temperaturaDia4 = 35.7;
        double temperaturaDia1 = 30.1;
        double soma1 = 0;
        double soma = 0;
        temperaturas[0] = 15.7;
        temperaturas[1] = temperaturaDia2;
        temperaturas[2] = temperaturaDia3;
        temperaturas[3] = temperaturaDia4;

        VetoresAula1 vetor = new VetoresAula1();

        vetor.ImprimiVetor();

        for (int i = 0; i < temperaturas.length; i++) {
            System.out.println("temperatura do dia " + (i + 1) + " é : " + temperaturas[i]);
        }

        for (int i = 0; i < (temperaturas.length - 1); i++) {

            soma1 += temperaturas[i];

        }
        System.out.println("a soma dos valores é : " + soma1);

        //CLONAR UM VETOR.
        double[] clone = temperaturas.clone();

        for (double valor : clone) {
            System.out.println(valor);
        }
        for (double valor : clone) {
            soma += valor;

        }System.out.println(soma);


        int[][] matriz= {{1,2,3},{4,5,6},{7,8,9}} ;

        System.out.println(matriz.length);

        for(int linha = 0;linha < matriz.length; linha++) {
            for(int coluna = 0;coluna < matriz.length; coluna++) {
                System.out.println(matriz[linha][coluna]);
            }
        }
*/
}


